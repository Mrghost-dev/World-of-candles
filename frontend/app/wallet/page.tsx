"use client";import{useEffect,useState}from"react";import{api}from"../../lib/api";import Link from"next/link";export default function Wallet(){const[m,setM]=useState<any>(),[tx,setTx]=useState<any[]>([]),[a,setA]=useState("100"),[method,setMethod]=useState("demo"),[msg,setMsg]=useState("");async function load(){setM(await api("/me"));setTx(await api("/wallet/transactions"))}useEffect(()=>{load().catch(()=>location.href="/login")},[]);async function dep(){try{let x=await api("/wallet/deposit",{method:"POST",body:JSON.stringify({amount:+a,method})});setMsg(`${x.reference}: ${x.status}`);load()}catch(e:any){setMsg(e.message)}}async function wd(){try{let x=await api("/wallet/withdraw",{method:"POST",body:JSON.stringify({amount:+a,method})});setMsg(`${x.reference}: ${x.status}`);load()}catch(e:any){setMsg(e.message)}}if(!m)return <main className="container">Loading...</main>;return <><nav className="nav"><Link className="logo" href="/">RAVIN<i>TRADING</i></Link><Link href="/dashboard">Dashboard</Link></nav><main className="container"><div className="pagehead"><div><div className="eyebrow">WALLET</div><h1>Funds</h1></div><strong className="green">${m.balance.toFixed(2)}</strong></div><div className="grid2"><div className="card"><h2>Deposit</h2><p className="muted">Demo deposits credit instantly. M-Pesa/PayPal remain pending until provider verification is configured.</p><div className="form"><input type="number" value={a} onChange={e=>setA(e.target.value)}/><select value={method} onChange={e=>setMethod(e.target.value)}><option value="demo">Demo</option><option value="mpesa">M-Pesa</option><option value="paypal">PayPal</option></select><button className="btn" onClick={dep}>Deposit</button></div></div><div className="card"><h2>Withdrawal</h2><p className="muted">Requests enter admin review.</p><button className="btn secondary" onClick={wd}>Request withdrawal</button>{msg&&<div className="result">{msg}</div>}</div></div><div className="card" style={{marginTop:18}}><h2>Ledger</h2><div className="table"><table><thead><tr><th>Reference</th><th>Type</th><th>Method</th><th>Amount</th><th>Status</th></tr></thead><tbody>{tx.map(x=><tr key={x.ref}><td>{x.ref}</td><td>{x.kind}</td><td>{x.method}</td><td>${x.amount.toFixed(2)}</td><td>{x.status}</td></tr>)}</tbody></table></div></div></main></>}
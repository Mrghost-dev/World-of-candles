"use client";import{useState}from"react";import{useRouter}from"next/navigation";import Link from"next/link";import{api}from"../../lib/api";export default function Login(){const[e,setE]=useState(""),[p,setP]=useState(""),[x,setX]=useState(""),r=useRouter();async function go(a:any){a.preventDefault();try{let u=await api("/auth/login",{method:"POST",body:JSON.stringify({email:e,password:p})});r.push(["admin","operations","compliance"].includes(u.role)?"/admin":"/dashboard")}catch(z:any){setX(z.message)}}return <><nav className="nav"><Link className="logo" href="/">RAVIN<i>TRADING</i></Link><Link href="/register">Create account</Link></nav><main className="container"><div className="card auth"><div className="eyebrow">SECURE ACCESS</div><h1>Sign in</h1><p className="muted">Authorized admin credentials automatically enter the restricted Master Admin dashboard.</p>{x&&<div className="flash error">{x}</div>}<form className="form" onSubmit={go}><label>Email<input type="email" value={e} onChange={a=>setE(a.target.value)} required/></label><label>Password<input type="password" value={p} onChange={a=>setP(a.target.value)} required/></label><button className="btn">Sign in</button></form></div></main></>}
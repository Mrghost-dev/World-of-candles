"use client";import{useState}from"react";import{useRouter}from"next/navigation";import Link from"next/link";import{api}from"../../lib/api";export default function Register(){const[n,setN]=useState(""),[e,setE]=useState(""),[p,setP]=useState(""),[x,setX]=useState(""),r=useRouter();async function go(a:any){a.preventDefault();try{await api("/auth/register",{method:"POST",body:JSON.stringify({name:n,email:e,password:p})});r.push("/dashboard")}catch(z:any){setX(z.message)}}return <><nav className="nav"><Link className="logo" href="/">RAVIN<i>TRADING</i></Link><Link href="/login">Login</Link></nav><main className="container"><div className="card auth"><div className="eyebrow">CLIENT ONBOARDING</div><h1>Create account</h1>{x&&<div className="flash error">{x}</div>}<form className="form" onSubmit={go}><label>Name<input value={n} onChange={a=>setN(a.target.value)} required/></label><label>Email<input type="email" value={e} onChange={a=>setE(a.target.value)} required/></label><label>Password<input type="password" minLength={8} value={p} onChange={a=>setP(a.target.value)} required/></label><button className="btn">Create account</button></form></div></main></>}